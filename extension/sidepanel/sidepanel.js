const API_URL="http://127.0.0.1:8000/api/capture",toggle=document.querySelector("#mining-toggle"),mode=document.querySelector("#mode"),status=document.querySelector("#capture-status"),expression=document.querySelector("#expression"),reading=document.querySelector("#reading"),meanings=document.querySelector("#meanings"),examples=document.querySelector("#examples");let miningMode=false;
function setStatus(message,isError=false){status.textContent=message;status.classList.toggle("error",isError)}
async function setMiningMode(enabled){miningMode=enabled;toggle.setAttribute("aria-pressed",String(enabled));toggle.textContent=enabled?"Stop mining":"Start mining";mode.textContent=enabled?"Mining mode is on. Select Japanese text on the page.":"Mining mode is off.";const result=await chrome.runtime.sendMessage({type:"SET_MINING_MODE",enabled});if(!result?.ok)setStatus(result?.error||"Capture setup failed.",true);}
function add(parent,tag,text,className=""){const element=document.createElement(tag);element.textContent=text;if(className)element.className=className;parent.append(element);}
function renderDetails(body){meanings.replaceChildren();examples.replaceChildren();for(const entry of body.entries||[]){const block=document.createElement("article");block.className="dictionary-entry";add(block,"h2",entry.dictionary);if(entry.parts_of_speech?.length)add(block,"p",entry.parts_of_speech.join(" · "),"meta");entry.senses.forEach((sense,index)=>{const section=document.createElement("section");section.className="sense";if(entry.senses.length>1)add(section,"p",`SENSE ${index+1}`,"sense-label");if(sense.glosses?.length){const list=document.createElement("ul");sense.glosses.forEach(gloss=>add(list,"li",gloss));section.append(list);}sense.notes?.forEach(note=>add(section,"p",note,"note"));sense.examples?.forEach(example=>{add(section,"p",example.japanese,"example");if(example.translation)add(section,"p",example.translation,"translation");});block.append(section);});meanings.append(block);}}
async function identify(text){const capturedText=typeof text==="string"?text.trim():"";if(!capturedText)return;setStatus("Identifying selection…");expression.textContent="—";reading.textContent="";meanings.replaceChildren();examples.replaceChildren();try{const response=await fetch(API_URL,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({text:capturedText})}),body=await response.json().catch(()=>({}));if(!response.ok){const cause=response.status===503?"Backend/Yomitan unavailable":response.status===422?"Invalid capture request":"Backend request failed";throw new Error(`${cause}: ${body.detail||response.statusText}`);}expression.textContent=body.expression;reading.textContent=body.reading||"Reading unavailable from Yomitan.";renderDetails(body);setStatus(body.dictionary_error||"Capture identified.",Boolean(body.dictionary_error));}catch(error){setStatus(error.message||"Backend unavailable.",true);}}
toggle.addEventListener("click",()=>setMiningMode(!miningMode).catch(error=>setStatus(`Capture setup failed: ${error.message}`,true)));chrome.runtime.onMessage.addListener(message=>{if(message?.type==="JAPANESE_TEXT_CAPTURED")identify(message.text);if(message?.type==="CAPTURE_DIAGNOSTIC")setStatus(`${message.stage}: ${message.error}`,true);});

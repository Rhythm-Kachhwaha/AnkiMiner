const assert=require("node:assert/strict"),fs=require("node:fs"),vm=require("node:vm"),context={globalThis:{}};vm.runInNewContext(fs.readFileSync("extension/content/capture-utils.js","utf8"),context);const {selectedText,captureMessage}=context.globalThis.AnkiMinerCapture;assert.equal(selectedText({rangeCount:1,toString:()=>"  映 画\n"}),"映 画");assert.equal(selectedText({rangeCount:0,toString:()=>"映画"}),"");const message=captureMessage("映画");assert.equal(message.type,"JAPANESE_TEXT_CAPTURED");assert.equal(message.text,"映画");console.log("capture-utils tests passed");
